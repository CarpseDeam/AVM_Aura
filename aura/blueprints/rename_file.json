{
  "tool_name": "rename_file",
  "description": "Renames a file by moving it to a new path. This is an alias for the `move_file` tool.",
  "parameters": [
    {
      "name": "source_path",
      "type": "string",
      "description": "The original path of the file to rename."
    },
    {
      "name": "destination_path",
      "type": "string",
      "description": "The new path for the file."
    }
  ]
}
